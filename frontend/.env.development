VITE_API_URL=http://localhost:8080/api
# false — реальный backend (приглашения между разными браузерами/вкладками)
# true — mock в памяти; регистрации синхронизируются через localStorage
VITE_USE_MOCK=false
